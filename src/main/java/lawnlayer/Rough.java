package lawnlayer;

// import org.checkerframework.checker.units.qual.A;
// import processing.core.PApplet;
// import processing.core.PImage;
// import processing.data.JSONObject;
// import processing.data.JSONArray;
import processing.core.PFont;

public class Rough {
    public static PFont font;
     

    public static void main(String[] args) {
        String[] fonter = font.list();
        for (String i: fonter){
            System.out.println(i);



            // if (level == "level1.txt"){

        // }
        // for (int i = 0; i<WIDTH; i+=SPRITESIZE)
        // this.image(this.concrete, i, TOPBAR);
        
        // for (int i = 0; i<WIDTH; i+=SPRITESIZE)
        // this.image(this.concrete, i, HEIGHT-SPRITESIZE);
        
        // for (int i = (TOPBAR+SPRITESIZE); i<HEIGHT; i+=SPRITESIZE)
        // this.image(this.concrete, 0, i);
        
        // for (int i = (TOPBAR+SPRITESIZE); i<HEIGHT; i+=SPRITESIZE)
        // this.image(this.concrete, WIDTH-SPRITESIZE, i);
        }
    }
}
